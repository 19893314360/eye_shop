# 开发任务拆分（第一版）

## 1. 当前已完成
1. 项目基础初始化（TS + Sass）。
2. 三端角色模型（客户、销售员、管理者）。
3. 页面骨架：
   - `pages/entry/index` 角色选择
   - `pages/home/index` 首页
   - `pages/workbench/index` 功能
   - `pages/stats/index` 统计
   - `pages/mine/index` 我的
4. 统一底部导航组件 `components/bottom-nav`。
5. 模块占位数据与基础权限分流（客户端不可看经营统计）。
6. P0 基础层：
   - `utils/request.ts` 统一请求封装
   - `services/auth.ts`、`services/auth-session.ts` 认证会话流程
   - `services/mock/server.ts` mock 接口
   - `store/app-state.ts` 全局状态与持久化
   - `types/common.ts`、`types/auth.ts` 公共类型
7. `npm.cmd run typecheck` 已通过。

## 2. 第二步（建议本周）
1. 进入 P1：销售主链路开发（进行中）。
2. 开发顺序建议：
   - `member`：新增会员
   - `sales`：普通开单
   - `payment`：收款
   - `delivery`：取件核销
3. 每个子模块均按“页面 -> service -> mock 接口 -> 提交校验 -> 类型检查”闭环执行。
4. 先保证流程可跑通，再补充复杂规则（优惠叠加、分期、退款等）。

## 2.1 当前阶段判断（更新）
1. P1 页面优先第一阶段：已完成。
2. 当前策略：继续补齐关键业务页面，再统一切换真实接口驱动。
3. 当前主要风险：页面与真实后端字段尚未对齐，联调前需补接口契约。
4. 项目决策（2026-04-15）：正式域名采购与备案延后到上线前，当前开发不以域名为阻塞项。

## 3. 模块拆分建议
1. `auth`：登录、角色、权限
2. `member`：会员档案、充值、积分
3. `sales`：开单、支付、取件
4. `after-sale`：售后与复查
5. `training`：视训方案、核销、提醒
6. `inventory`：库存、盘点、调拨
7. `purchase`：采购、入库、退货
8. `report`：经营统计、排行
9. `settings`：公告、打印模板、设备配置

## 4. 接下来我建议先做
1. 已完成：`新增会员` 页面（字段、校验、提交）与 `member service`。
2. 已完成：`普通/验光开单` 页面与 `sales service`。
3. 已完成：`收款`、`顾客取件` 页面与订单状态流转接口。
4. 已完成：`配镜记录` 列表页和 `订单详情` 页面。
5. 已完成：`售后页面`（待回访/待复查/已处理）与订单详情售后入口跳转。
6. 已完成：`服务预约`、`视训中心`、`库存查询` 页面与模块入口跳转。
7. 已完成：`会员查询`、`视力档案`、`财务汇总` 页面与模块入口跳转。
8. 已完成：`新增验光`、`采购管理`、`系统设置` 页面与模块入口跳转。
9. 已启动：接口驱动改造第一批（`auth/member/order` 已完成 DTO 映射与兼容层）。
10. 已完成：接口驱动改造第二批（`vision/purchase/settings/finance`）。
11. 已完成：联调配置入口（系统设置页可切 `Mock/真实后端` 并配置 `API Base URL`）。
12. 已完成：《接口联调清单（驱动层）》文档。
13. 已开始：真实后端联调准备（请求层已兼容 envelope/raw 两种响应格式）。
14. 已完成：请求字段风格切换（`snake/camel/both`）联调配置能力。
15. 已完成：主链路联调执行按钮（写入链路）与逐步日志输出。
16. 已完成：联调错误信息补充实际请求 URL，便于快速定位 `INVALID_PATH/404`。
17. 已完成：联调执行前自动保存运行时配置，减少误操作成本。
18. 已完成：鉴权初始化自动校验 `Mock/真实` 模式与 token 匹配，不匹配自动重登。
19. 已完成：功能收口 — 补齐所有菜单页面对应路由（个人资料、联系客服、门店管理、人员与权限、更新日志、设备管理、教学视频）。
20. 已完成：管理端统计报表页面重构（经营状况总览/详情/收款/欠款 + 销售员排行榜，支持今日/本月筛选）。
21. 已完成：ES2020+ 语法兼容性修复（`?.` → `&&` 短路、`??` → 三元、`30_000` → `30000`，tsconfig target 降至 ES2017）。
22. 已完成：自动登录与角色识别体系（`app.ts` onLaunch 自动路由、`auth-driver` Mock 角色检测、员工绑定表模拟）。
23. 已完成：人员与权限管理增强（新增员工弹窗、微信绑定/解绑、角色分配、门店绑定）。
24. 已完成：客户端售后申请页面（退货/换货/维修/退款，与售后追踪页双向联动）。
25. 已完成：售后追踪页增强（整合客户端申请数据，回访/复查操作同步更新申请状态）。
26. 已完成：联系客服页面（门店电话、地图导航、FAQ 快捷入口）。
27. 已完成：预约管理完善（路由参数预填、客户自动填充、到店标记）。
28. 已完成：设备管理页面（打印机/收款盒子/扫码枪，搜索筛选、状态监控、设备文档）。
29. 已完成：更新日志页面（V1.0.0 / V1.1.0 / V1.2.0 版本记录展示）。
30. 已完成：入口页降级为角色切换页（自动登录优先，手动切换为辅）。
31. 已完成：加工管理页面（开始加工/进度更新/完工入库，FR-033）。
32. 已完成：销售员个人业绩视图（stats 页面角色分流，销售员可见"我的业绩+全店排行"）。
33. 已完成：视训方案创建/发布功能（新建方案弹窗表单，同步创建核销记录）。
34. 已完成：镜片价目册页面（品牌/折射率/膜层筛选，SKU 价格查询）。
35. 已完成：顾客端小程序码页面（生成/保存/分享客户端小程序码）。
36. 下一步：真实联调按清单分阶段推进，补异常路径和回归测试。
37. 上线前统一完成：正式域名采购/备案、云托管自定义域名绑定、小程序合法域名切换。
